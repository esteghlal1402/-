# ربات همه کاره - Versatile Robot
# وابستگی‌های مورد نیاز

# کتابخانه‌های استاندارد پایتون (نیازی به نصب ندارند)
# os, sys, json, time, random, math, datetime, re, subprocess

# کتابخانه‌های اختیاری برای بهبود عملکرد
# colorama>=0.4.4  # برای رنگ‌بندی متن در ترمینال
# rich>=13.0.0     # برای نمایش بهتر متن و جداول
# requests>=2.28.0 # برای درخواست‌های HTTP (در صورت نیاز)

# برای اجرای ربات فقط پایتون 3.6+ کافی است
# python>=3.6